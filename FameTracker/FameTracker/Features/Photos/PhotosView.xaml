<?xml version="1.0" encoding="utf-8"?>

<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:resources="clr-namespace:FameTracker.Core.Resources;assembly=FameTracker"
             x:Class="FameTracker.Features.Photos.PhotosView"
             Title="{x:Static resources:Strings.Photos}"
             IconImageSource="{OnPlatform iOS=photos.png, Android=ic_photos}"
             BackgroundColor="#202020">
    <ContentPage.Content>
        <ScrollView>
            <StackLayout>
                <StackLayout Padding="17" BackgroundColor="#161616" VerticalOptions="FillAndExpand"
                             HorizontalOptions="FillAndExpand">
                    <Label Text="New Photos" TextColor="#f5f5f5" HorizontalTextAlignment="Center"></Label>
                </StackLayout>
            </StackLayout>
        </ScrollView>
    </ContentPage.Content>
</ContentPage>